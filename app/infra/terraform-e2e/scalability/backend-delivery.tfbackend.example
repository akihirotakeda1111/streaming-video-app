# Copy to a private operator runtime directory and pass its absolute path to
# `terraform init -backend-config`. Never store state under this source tree.
path = "/var/lib/streaming-video-e2e/scalability/delivery/terraform.tfstate"
